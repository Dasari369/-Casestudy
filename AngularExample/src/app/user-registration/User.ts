export class User{
      userID !:number;
	  userName !: string;
	  password !:string;
	  email !:string;
	  firstName !:string;
	  lastName !:string;
	  contactNumber !:number;
	  role !:string;
	  isActive !:Boolean;
	  dOB !:Date;
	  createdOn !:Date;
}